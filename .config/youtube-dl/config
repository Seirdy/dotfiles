--add-metadata
--fixup detect_or_warn
--all-subs
--embed-subs
# --embed-thumbnail # not yet supported for many formats
--console-title
--prefer-free-formats
# speed things up a bit
--external-downloader aria2c
--audio-quality 0
--xattrs
# --merge-output-format mkv
# choose format only based on codec, height, and FPS. Probably overkill
--format '(bestvideo[vcodec=av01.0.08M.08]/bestvideo[ext=webm]/bestvideo)+(bestaudio[acodec=opus]/bestaudio)/best'
