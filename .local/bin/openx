#!/usr/bin/env dash

if [ "$MACHINE" = "Linux" ]; then
	xdg-open "$@" >/dev/null 2>/dev/null
elif [ "$MACHINE" = "Darwin" ]; then
	nohup open "$@" >/dev/null 2>/dev/null
else
	echo "Unsupported machine. Your \$MACHINE is '$MACHINE'"
fi

# vim:filetype=sh
