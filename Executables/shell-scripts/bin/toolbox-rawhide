#!/usr/bin/env dash

# switch to rawhide; keep my $HOME but make the rest of the system (besides the kernel) Fedora Rawhide by entering a podman container.
unset LC_ALL LANG
toolbox run --release f32 "$(command -v zsh)"
# vi:ft=sh

