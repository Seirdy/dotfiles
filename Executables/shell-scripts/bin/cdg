#!/usr/bin/env dash
# cd to a git repo managed by ghq
if [ -z "$1" ]; then
	cdto=$(ghq list | fzf)
else
	cdto=$(ghq list | fzf -q "$@")
fi
# shellcheck disable=SC2154
[ "$cdto" = '' ] || cd "$GHQ_ROOT/$cdto" || return 1

# vi:ft=sh

