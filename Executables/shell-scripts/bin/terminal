#!/usr/bin/env dash
if [ "$XDG_SESSION_TYPE" = 'wayland' ] || [ -n "$SWAYSOCK" ] || [ -n "$WAYLAND_DISPLAY" ]; then
	if [ $# -gt 0 ]; then
		alacritty -e "$@"
	else
		alacritty
	fi
else
	st "$@"
fi
# vi:ft=sh
